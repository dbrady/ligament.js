beforeEach(function() {
  this.addMatchers({
// left over from Jasmine examples
//     toBePlaying: function(expectedSong) {
//       var player = this.actual;
//       return player.currentlyPlayingSong === expectedSong
//           && player.isPlaying;
//     }
  })
});
